import React from 'react';

const App = () =>{
    return(
        <div id='app-container'style={styles.appContainer}>
        </div>
    );
};

const styles = {
    appContainer: {
        backgroundColor: '#000'
    }
};

export default App;